------------------- IMPORTANT! ---------------------------

In order to run the PDF to image reader pdf2image you need to install a poppler package in your windows machine and add the path to environment variable.
Else the program wouldnot run locally.

URL : 

https://github.com/oschwartz10612/poppler-windows (Latest version)

You might need to restart the IDE (VS code after installing on windows and setting the environment variable)